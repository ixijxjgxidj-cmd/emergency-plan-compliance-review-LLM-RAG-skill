# 应急预案合规审查（Claude Code 入口）

本目录是一个 skill 包，入口是根目录的 [SKILL.md](SKILL.md)。

开始任何工作前：

1. 读 `SKILL.md`（适用范围、绝对边界、工作流、验收标准）。
2. 读 `prompts/master.md`（总控）。
3. 执行某阶段前读该阶段的 `prompts/**/prompt.md`，只做该阶段的事。

关键约束（详见 SKILL.md 第 2 节）：

- 只审法律法规问题，不做实操/资源/经验类评价。
- 法规依据只能来自用户放入 `./laws/` 的文件，不得凭记忆生成条文。
- 联网核验只核验已发现问题，不新增问题、不改编号。
- 每条款独立审查，禁止抽样、禁止"同上"。
- 每阶段必须落盘，禁止口头宣布完成。

联网能力：仅 Agent7（核验已发现问题）需要联网，优先使用本环境自带的 WebSearch / WebFetch 工具；若不可用，再按 `./output/model_config.json` 调用具备联网能力的模型 API。核验不到记 `unverified`，不得编造。
